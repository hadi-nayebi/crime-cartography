# watcher solutions playbook (grows every resolution)

The note-watcher reads this BEFORE resolving (known categories first) and
appends AFTER each resolution: the category, the ROOT CAUSE (not the symptom),
how wide the category sweep went, and the lesson. This file is the "getting
better at solving" half of the critic↔watcher ratchet. Rule of the house:
surface-patching one instance of a systemic issue is a failure mode — fix the
producing layer (template/engine/pipeline/routine) and sweep every instance.

## categories solved (category | root cause | sweep breadth | lesson)
- routines-sweep-foreign-work | root cause: `git add -A` in every routine SKILL's commit step + GIT POLICY template | sweep: 7 SKILL files, 11 occurrences replaced with explicit-file adds | lesson: shared-repo automation must never blanket-stage; observed twice tonight (agent work swept into "note:" commits 72f6f1c, 0456d53/3270cb7)
- no-data-crowned-as-extreme (safest/least/best of a ranking) | root cause: engine derive.ts `hoodRanking` sorted by groupATotalAll with no data-presence guard — a spatial-join artifact (allTotal===0, zero incidents in EVERY category every month) is indistinguishable from a real 0 and sorts to the tail, so it can be crowned "safest" | sweep: engine fix (filter allTotal===0 out of hoodRanking + disclose in Reveal, merged as PR #4 w/ hoodNoDataCount + conditional caveat) + all 20 cities — CORRECTION (2026-07-20, producer): the initial full-timeline scan said "only Atlanta"; re-sweeping with the render's ACTUAL mapWindowMonths slice found THREE affected: atlanta (3 hoods → Bankhead Courts:3), milwaukee-wi (Brown Deer Park → Zoo:1), washington-dc (Observatory Circle → Rock Creek Park:15 — SHIPPED mp4 shows the artifact; re-render note + confidence blocker filed) | lesson: ANY derived superlative over sparse units (min/least/safest/fewest) must exclude true-no-data units — "absence of data" ≠ "best value"; keep genuinely-low-but-present units (allTotal>0) so you don't hide a real safe area. Guard belongs in the engine, not per-config, so it can't recur for future cities. SECOND lesson from the correction: a sweep must mirror the RENDER'S data path exactly (same window slice, same join) — scanning full-span normalized data missed two window-only zeros.

- publish-candidate-thumbs-missing-on-new-renders | root cause: the studio generates the 6 publish-thumbnail candidate frames LAZILY (ensureThumbs in pipeline/dashboard/server.mjs — only fires on a publish-preview load) AND `videos/**/thumbs/` is gitignored, so any render after the first hand-warmed batch shows an empty candidate picker until someone opens its modal — original 8 were warmed, 12 newer renders never were (channel/growth flagged it a hard publish blocker) | sweep: all 12 cohort cities pre-warmed in one pass (72 frames, ffprobe-valid 1280x720) via the studio's exact ffmpeg recipe; frames stay local/gitignored like the originals | lesson: a lazily-generated + gitignored artifact that a gate depends on is invisible debt — pre-warm it at the PRODUCING step (render landing), not at consume time. Also: when a route is namespaced under a forbidden verb (/api/publish/...thumbs is frame extraction, not upload) the no-publish guard still blocks it — run the underlying tool (ffmpeg) directly for identical, honest output. Filed HARNESS 'requested' so the producer/driver self-warms future renders.

- video-page-no-wayfinding (operator lands on a video page, doesn't know what to do / what "review" or "verify" means / how to publish / how to raise the score) | root cause: the detail view was an INFORMATION display (8 cryptic stage lights + confidence + meta + a feedback box) with no stage-aware "what's my next move?" instruction; the flow rules (Verify is a manual light flipped by Approve, Publish needs all-green, confidence is advisory-not-a-gate) lived only in server.mjs + producerNotes, never on screen | sweep: fixed at the ENGINE layer (pipeline/dashboard/index.html renderGuide) so ONE change covers ALL 20 video pages — a plain-language "Next step" guide: Step N/8 + one-line action + explanation + the full journey as a who-does-what checklist, stage-branched (automated stages="runs automatically, leave a note"; review=defines review + ✓Approve→Verify; all-green=⤴Publish CTA; published=▶Watch); relabeled the Approve button "Approve — turn on Verify"; surfaced Confidence as advisory with blockers as the to-do that lifts it. Verified in-browser + DOM across all 8 branches | lesson: a multi-stage operator tool must TELL the user their single next action at each state, not just DISPLAY status and assume they can decode it — wayfinding is content, not chrome. When a note says "applies to all videos", the fix belongs in the shared render path (index.html/engine), which makes the category sweep automatic. Owner-facing flow rules must be explained inline where the action happens, never left implicit in server code.

- owner-approve-leaves-satisfied-blocker-stale | root cause: confidence.json `blockers` is hand-maintained free text with NO code path to strip a blocker when its condition is met — so a re-render blocker kept open "pending owner re-approval" stays listed even after the re-render lands AND the owner re-approves (the operations-critic 15:13 note is the systemic version of this) | sweep: reconciled ONLY the one blocker causally gated on this approve (Boston's re-render blocker — producer explicitly wrote "verify light owner-gated pending re-approval"); did NOT touch the watch-through blocker (approve ≠ audio watch-through) nor other cities' ledgers (that broader auto-clear is the open critic note's job) | lesson: when resolving an owner APPROVE, don't just log evidence — check whether that approve was the GATING EVENT the producer set up for an open blocker, and if the artifact condition is also met, strip it (the ledger otherwise drifts and inflates priorityOf). Honesty guard: only clear a blocker whose condition is actually satisfied AND that this approve was gating; leave genuinely-separate advisory blockers (audio watch-through) open. Score must NOT bump on a re-approval of an already-counted design gain — that would double-inflate.

- config-field-missing-not-empty (a publish/dashboard feature depends on a youtube.json/config field the authoring step doesn't guarantee) | root cause: the producer's title-authoring step lands youtube.json but never guarantees `titleOptions`, so 3 late configs (denver-co/detroit-mi/milwaukee-wi) shipped with the field ABSENT while 17 carried 2 — the publish modal's title picker then had no alternates | sweep: authored exactly 2 alternates for ALL 3 gap cities (decline-% hook + raw-endpoint hook), built ONLY from producer-verified hook/punchline figures, each ≤100 chars, none crossing the FBI↔incident measurement seam; verified vs each config + confidence.json; SURGICAL text insert after the `title` key (json.dump re-serialize was reverted first because it expanded the compact `chapters` objects → 96-line churn) | lesson: (1) content that must be honest+per-city (verified alternate titles) can't be auto-generated by a template, but the authoring routine CAN be made to REQUIRE the field so the gap is caught at author time — fix filed to HARNESS 'requested'; (2) never re-serialize a hand-formatted JSON to add one key — `json.dump(indent=2)` reflows every compact sub-object; do a surgical text insert and diff-check (expect ~4 insertions, not ~90); (3) a titleOptions/superlative honesty check: an alternate title must never state a % or endpoint pair that spans the source seam — lead on the same-measure decline only.
- render-clears-only-one-of-N-satisfied-blockers (partial-clear drift; sub-case of owner-approve-leaves-satisfied-blocker-stale) | root cause: ONE re-render can satisfy MULTIPLE re-render blockers at once — DC's 2026-07-20 00:43 render carried BOTH the zero-count 'safest' fix AND the seamExplain 'WHY THE JUMP?' fix — but confidence.json blockers are hand-cleared one at a time, so the producer's verify pass logged '[blockers cleared: 1]' and stripped only the zero-count, leaving the seamExplain re-render blocker stale though the SAME artifact satisfied it | sweep: caught while resolving DC's owner APPROVE — extracted a still @96s proving the seam card renders the DC seamExplain copy, cleared the stale blocker, recorded the approve as verify evidence; DC was the only open owner APPROVE (boston done prior run) | lesson: when a re-render lands, DON'T clear blockers from memory one-by-one — enumerate EVERY blocker whose condition that single render satisfies and strip them all; a '[blockers cleared: 1]' after a render that fixed 2+ things is a drift smell. An owner APPROVE is the natural checkpoint to catch it: cheaply verify the current mp4 actually renders each 're-render to pick up X' fix (one still) before signing the ledger. Honesty guard held: only cleared the blocker whose artifact I visually confirmed; left watch-through (separate manual gate, boston precedent) and society=0 (genuine honesty item) open.
- optional-authored-copy-falls-back-to-generic-engine-default (a per-city honesty/quality explainer the engine renders is only city-accurate when an OPTIONAL config field is authored; un-authored cities silently show a generic default) | root cause: `copy.seamExplain` (the trend measure-seam "why the jump?" card in FullTrend.tsx) is optional hand-authored copy with no authoring guarantee — 14/20 cities had a verified one, 6 (chicago/grand-rapids/minneapolis/philadelphia/san-francisco/seattle) shipped without and fell back to the engine's generic default ("newer, broader incident-based system"), honest but not city-tailored and slightly loose for the "all recorded incidents/offenses" cities | sweep: authored a verified city-specific seamExplain for ALL 6 (grounded ONLY in each city's committed trend.json note + eras — FBI index → that city's own incident/NIBRS measure; the step is the ruler widening, not a crime jump), surgical insert after copy.quizQuestion (no reflow), 424-447 chars (within proven-render range); all 20 now carry one; re-render blocker + history added per city in confidence.json (copy baked at render time; no score change); HARNESS 'requested' filed so authoring must emit seamExplain for any seamed trend | lesson: when the engine renders a per-city explainer from an OPTIONAL config field, "has a box" ≠ "has the RIGHT box" — a generic default masks the gap. Sweep by checking which cities actually authored the field, not whether the feature renders. Owner "apply to all videos" note = fill EVERY instance of the optional field (verified per city, never templated because it must be honest) + guard the authoring step upstream so future cities can't ship on the generic fallback. Never re-serialize hand-formatted JSON to add one key — surgical text insert + diff-check (expect +1 line/config).

- no-thumbnail-standard (thumbnails are ad-hoc single frames / one-off hand jobs — "bare minimum", inconsistent, and 0/20 have a DESIGNED composed thumbnail.jpg) | root cause: there was no thumbnail-composition routine at all; the studio only ever copies ONE raw frame to thumbnail.jpg on publish (server.mjs), so quality/consistency depended on whoever hand-made one (only boston had) | sweep: built the producing layer — pipeline/publish/compose-thumbnail.py, a reusable "theme 1: map + stats" composer (Python/PIL, the only rasterizer available — no ImageMagick/rsvg/node-canvas) driven ONLY by verified config figures (hook.stat/hook.line/copy.cityName) + optional per-city verified thumb.json (frame/crop/yearRange/kicker/safest/busiest); ran --all → 20/20 now ship a designed thumbnail.jpg (19 had none). Boston got the full owner-requested spec (−71%, 1985–2026, busiest=South End & safest=Charlestown, all from its own config.annotations). QA'd all 20 via a single PIL contact sheet (efficient — one image to eyeball the cohort), which caught 3 systemic sub-defects: (a) baked annotation/trend cards bleeding into the map on 4 cities (buffalo/cincinnati/grand-rapids/milwaukee) → thumb.json frame=t210 (clean center map; t155 is a transition card not a map, t290 late-sweep sometimes shows an annotation); (b) source credits truncating mid-word / dangling on connectors → strip parenthetical license + trailing prepositions + word-boundary cut; (c) long hook lines (seattle/washington-dc) truncated mid-phrase by a 2-line cap → shrink-to-fit font so the WHOLE verified phrase always shows | lesson: (1) a QUALITY-not-just-presence note ("make it the BEST / define a standard") = build the producing routine, don't hand-make one instance; the sweep is running that routine across the cohort. (2) For honesty across a sweep, only draw figures GUARANTEED verified per city (hook.stat/hook.line/cityName exist in all 20 configs) and gate richer per-city figures (neighborhood names) behind an explicit verified spec that the composer OMITS when absent — never recompute a superlative for a thumbnail (same hoodRanking window-slice trap as the no-data-superlative category). (3) Build a contact sheet to QA a cohort visually in one look — small tiles surface annotation-bleed / truncation / low-contrast that per-city viewing misses. (4) PIL beats ffmpeg drawtext for designed multi-element layout; check the rasterizer inventory first. Filed HARNESS: driver/producer must (re)compose on render-landing + author verified thumb.json neighborhoods per city.

- engine-hardcodes-one-city's-taxonomy-term-as-universal (a first-city term baked into the shared engine mislabels every later city whose source uses a different standard) | root cause: the engine was built for Grand Rapids (NIBRS) and hardcoded the NIBRS term "Group A" as a generic synonym for "the counted crime total" in surface/remotion/src (Quiz/TimelineChart/Reveal + a dead EraTransition no-trend line) — AND the same habit leaked into non-NIBRS cities' AUTHORED config annotation/quiz text — so ~half the cohort labeled its data with a NIBRS standard the agency doesn't use (Buffalo=10 major-crime types, Cincinnati=STARS, Detroit=RMS, "all recorded incidents" cities) | sweep: (1) ENGINE — added optional copy.countTerm; routed every on-screen occurrence through `${term} crime(s)` with a NEUTRAL default "reported" (so a config that omits the field can NEVER assert a standard by omission — future-city recurrence structurally impossible), tsc clean, verified via reveal+quiz stills for all 3 variants (Group A / reported / major). (2) CONFIG — set countTerm on all 20, verified per-source against each city's trend.json recent-era label + PROVENANCE nibrs_crimeagainst: "Group A" ONLY for the 10 native-NIBRS-Group-A sources, "major" for Buffalo, "reported" for the 9 that merely map their own categories into persons/property/society via the NIBRS convention; de-labeled the authored "Group A" text in the 6 non-native configs that had it. (3) re-render blocker + history on all 20 (label baked at render time; no score change). HARNESS filed: authoring must emit a verified countTerm + never write "Group A" into a non-NIBRS city's text | lesson: (1) when a note names ONE component ("Quiz.tsx hardcodes X"), the real category is "engine asserts a taxonomy" — grep the WHOLE engine AND the configs, because a term hardcoded in the engine is usually ALSO hand-copied into authored copy. (2) For a term that's false-if-wrong, make the ENGINE DEFAULT the neutral always-true value and opt IN to the specific standard — a default that can be false-by-omission (critic proposed default "Group A") fails the honesty invariant for any future city that forgets the field; a neutral default cannot. (3) The authoritative per-city taxonomy signal is the trend.json era label + PROVENANCE's native-field check, NOT the looser methodFootnote wording (Dallas's footnote said only "DPD incidents" but its era label + provenance proved native NIBRS → keep "Group A"; a footnote-only read mislabeled it "reported"). (4) "Group A"/NIBRS is safe to UNDER-state (generic "reported crimes" is true for NIBRS data) but never safe to OVER-state (calling STARS/RMS/all-incidents data "Group A" is false) — when unsure, use the neutral term.

- published-state-rendered-from-local-artifacts-not-live-truth (a PUBLISHED video shows stale/wrong data — old privacy, our composed thumbnail — because the dashboard reads local pre-publish files, never the live platform state) | root cause: the studio catalog fetched only `videos.list?part=statistics` and rendered the published card/detail from the LOCAL youtube.json (`privacyStatus`) + our LOCAL composed `thumbnail.jpg` + config title; once the owner flips a video to public and picks a YouTube-generated thumbnail on YouTube, those locals are stale/wrong, so a live public video showed as "private" with a thumbnail nobody published — the "unfair UI" the owner flagged | sweep: ENGINE-level in pipeline/dashboard (covers ALL published videos, now + future): server.mjs `ytStats` now fetches `part=snippet,statistics,status` (still 1 quota unit, cached 10min) → `youtube.live {title,thumb,privacyStatus,publishedAt}`, with local `privacyStatus`+`composedThumb` as offline fallbacks; index.html published card + detail render the REAL live thumbnail (onerror→composed), a privacy-aware LIVE/PRIVATE/UNLISTED tag + `visibility` line + live title, and a live-vs-"your designed" thumbnail compare with an A/B-testing note (composed thumbnail retained for when YouTube thumbnail testing turns on). Verified end-to-end: /api/catalog reports boston-ma+DC as privacyStatus=public with real i.ytimg thumbnails (both were stale "private" locally). HARNESS filed: state-sync routine must also write the live privacy/thumbnail back into youtube.json so the committed record stops drifting | lesson: (1) once an asset goes live on an external platform, the platform is the source of truth — render its state from the live API, not the local artifacts that produced it; a local record captured at publish time is a snapshot, not the current state. (2) A "fair UI for published videos" request = mirror live reality (real thumbnail/title/privacy) AND stop dressing a done, live video in pre-publish production chrome. (3) The DASHBOARD engine (index.html/server.mjs) is the shared render path — one change is the whole category sweep, exactly like the wayfinding-guide fix. (4) videos.list is 1 quota unit regardless of parts, so enriching from statistics→snippet+statistics+status is free; cache it. (5) When the owner mentions a not-yet-available future feature (A/B thumbnail testing), acknowledge + retain the asset for it — don't build the unavailable feature, and don't discard the work.

- outward-write-after-async-platform-op-fires-once-and-is-silently-swallowed (a metadata write to an external platform right after a long async op fails on the processing race, is best-effort+silent, and has no re-apply path — so the asset lands incomplete and the owner fixes it by hand off-platform) | root cause: the publish flow set the custom thumbnail (YouTube thumbnails.set) EXACTLY ONCE, immediately after videos.insert, best-effort and silent — but a freshly-uploaded video is still PROCESSING, so the call hits the transient 'video not found/processing' race and fails; the video shipped (private) with no custom thumbnail and the ONLY recovery was a manual YouTube-Studio upload + flip-to-public (the owner's exact experience: "the thumbnail never landed … I was only able to upload the thumbnail after"). No studio-side re-apply existed. | sweep: shared dashboard engine only (server.mjs is the SOLE thumbnails.set path in the tree → one fix = all 20 cities): (1) applyThumbnail() retries the transient race with backoff, treating 403 unverified / 400 bad-image as PERMANENT (no pointless retries) instead of one silent shot; (2) new guarded POST /api/publish/<slug>/setthumb + pushThumbnail() re-applies the committed thumbnail.jpg to an already-uploaded video (guard returns 409 on an unpublished city — never calls YouTube without a videoId, verified); (3) the thumbnail action now sits WITH the publish buttons in all THREE surfaces (index.html): publish-result retry when it didn't land, already-published modal, and the published-video detail guide — so the owner never leaves for YouTube Studio. Restarted crime-studio.service + curl-verified (catalog/publishPreview shapes intact, setthumb 409 guard, served page carries all wiring); no upload triggered by the watcher. | lesson: (1) an outward metadata write that follows a long ASYNC platform op (upload/transcode/index) must assume the resource is not yet ready — RETRY the transient race with backoff, and classify permanent failures (auth/eligibility/bad-input) so you don't retry them. (2) best-effort + silent = the failure is invisible until the owner notices the asset is wrong; surface a miss LOUDLY and give a one-click RE-APPLY on the same surface so recovery never means leaving the tool. (3) "include X with the publish buttons" = the action must live on the publish surface at publish time AND as a post-op re-apply, not be a fire-once side effect. (4) sibling to published-state-rendered-from-local-artifacts-not-live-truth: both are "the dashboard engine is the one shared path" — server.mjs/index.html fix IS the whole cohort sweep.

- dashboard-metadata-field-null-collapses-badge-to-a-generic-default (an operator-facing per-video badge/label is only meaningful when an OPTIONAL config field is authored; un-authored → every card shows the same derived fallback, so the "experiment points" the owner wanted to compare are indistinguishable) | root cause: `theme.name` was null on all 20 configs (grand-rapids had no theme block at all) because the config-authoring step never emits it AND no theme-name vocabulary existed — so the dashboard themeBadge (index.html) fell back to the luminance-derived mode, rendering 19 identical "dark" badges + grand-rapids none | sweep: DEFINED the missing spec (theme-name vocabulary in DECISIONS.md, 2026-07-20 section) + set a distinct, HONEST `theme.name` on all 20 (each DERIVED from that city's real committed `theme.colors`/`catColors` — moss/rust/sage/steel/brass/ember/clay/cobalt/glacier/iron/neon/jade/cyan/wheat/amethyst/honey/iris/sunset/tide/marble, never invented) + gave grand-rapids a full theme block whose colors EQUAL the engine defaults (theme.ts) for badge parity with ZERO render change + filed the authoring guard (HARNESS 'requested'); verified live via /api/catalog (20/20 distinct names, GR mode now "dark") | lesson: (1) same class as config-field-missing-not-empty and optional-authored-copy-falls-back-to-generic-engine-default — "has a fallback" ≠ "has a real value"; a null optional field silently collapses a per-item signal to a shared default, so a batch of distinct things reads as identical. Fix the SPEC (vocabulary) + fill EVERY instance + guard the authoring step upstream. (2) CHECK THE CONSUMER FIRST to size the blast radius: `theme.name` is read ONLY by the dashboard (index.html/server.mjs), never by the engine (applyThemeOverrides reads only colors/catColors) → the whole sweep is metadata-only, NO re-render, no confidence blocker. When adding a block to a bare config (grand-rapids), mirror the ENGINE DEFAULTS exactly so the render is provably byte-identical — never introduce new colors under the guise of "parity" (that's a silent re-render + design change). (3) For a label field, honesty = the name must accurately describe the config's own colors (never "light" for a dark bg); it never appears on-screen so it carries no incident-data risk — but a mislabeled palette would still be a lie to the operator. (4) Part of the owner's ask (≥1 light-mode city for the light/dark axis) required a real re-render + design call → flagged for producer, NOT done in a metadata run; resolving the config-field-null half is still a complete, honest resolution of the note. Surgical JSON inserts only (+1 line/config, GR +19), never re-serialize.

## open hypotheses (suspected systemic causes not yet confirmed)
- seattle-era-label-underspecified: seattle's trend.json recent-era label reads "SPD — all recorded offenses" but PROVENANCE confirms it's native NIBRS Group A (`nibrs_crime_against_category`); so its reveal correctly says "Group A crimes" while its on-screen era legend says the looser "all recorded offenses" — not a falsehood, but a within-video wording mismatch. Suspected category: trend-era-label authoring uses looser phrasing than the source's actual taxonomy for some cities. Confirm by auditing all 20 era labels against PROVENANCE native-field presence; tighten labels where the source is native NIBRS but the label omits it.
