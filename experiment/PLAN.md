# Operating plan (set 2026-07-18)

## Month (to ~2026-08-18)
- **W1:** Batch-1 complete → all 20 at confidence 100 → same-day cohort publish
  on Earth One. Playlist: "US Cities · Crime, Mapped".
- **W1–W2:** Monitor window (retention curves, CTR, AVD, comments) →
  `RESULTS-batch1.md` → lock winning feature levels (chart form, hook, palette
  family, music family, duration, map emphasis).
- **W2–W3:** Batch-2 (cities 21–40) selected to cluster TX / CA / OH / NY / TN;
  produced with winning levels + exploration slice; NYC & LA re-checked.
- **W3–W4:** First **state comparative** video (first state to reach 5 cities)
  with on-screen pull/transform/normalize/join methodology — new playlist.
  Batch-2 cohort publish. Co-variable prototype (libraries or churches overlay)
  built and honesty-reviewed — publish only if it meets the bar.

## Week (to 2026-07-25)
- Finish 11 remaining batch-1 pipelines + my per-city number verification.
- Finish engine dims: hook variants, 270s cut, map emphasis, MethodDiagram
  (data-lineage scene) → lock `matrix.json` (20 vectors, near-twin pairs).
- Configs/identities/listings ×12 → music ×12 → renders ×12 + re-render ×8
  (seam explainer + diagrams everywhere) → encode verification.
- Close all ledger blockers → 20 × confidence 100 → **cohort publish** →
  monitoring begins.

## Day (rolling)
- Advance builders/verification/configs as agents return; keep GPU chains
  saturated (music → renders, serialized).
- Act on every studio note same-day; ledger updated with evidence only.
- Commit + push at each milestone; ACTIVE.md always resumable.

## Briefings
Channel briefing to hadinayebi@earthone.life **every 8 hours**: scores &
blockers movement, videos produced/verified/published, studio feedback handled,
batch progress vs this plan, next-8h intent, risks.
